# 🚀 Быстрый деплой на Render.com - пошаговая инструкция

> **Важно:** Render не поддерживает MySQL напрямую. Мы будем использовать бесплатный MySQL от PlanetScale или Railway.

---

## ✅ Шаг 1: Код уже на GitHub

Ваш код уже запушен на GitHub: https://github.com/25angel/autoservice

---

## 🗄️ Шаг 2: Создание бесплатной MySQL базы данных

### Вариант А: PlanetScale (рекомендуется, самый простой)

1. **Зайдите на https://planetscale.com**
2. **Нажмите "Sign up"** и зарегистрируйтесь (можно через GitHub)
3. **Создайте базу данных:**
   - Нажмите "Create database"
   - Name: `autoservice`
   - Region: выберите ближайший (например, `us-east`)
   - Plan: выберите **"Hobby"** (бесплатный)
   - Нажмите "Create database"
4. **Получите данные для подключения:**
   - После создания БД нажмите "Connect"
   - Выберите "General" → "PHP"
   - Вы увидите строку подключения вида:
     ```
     mysql://username:password@host:port/database
     ```
   - **Скопируйте эту строку!** Она понадобится позже
5. **Импортируйте данные:**
   - В PlanetScale Dashboard найдите вкладку "Console"
   - Или используйте команду (замените данные на свои):
     ```bash
     mysql -h HOST -u USERNAME -p DATABASE < database/schema.sql
     ```
   - Введите пароль, когда попросит

### Вариант Б: Railway (альтернатива)

1. **Зайдите на https://railway.app**
2. **Зарегистрируйтесь** (можно через GitHub)
3. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Provision MySQL"
4. **Получите данные подключения:**
   - В настройках MySQL найдите "Connect"
   - Скопируйте переменные окружения или строку подключения
5. **Импортируйте данные** через MySQL клиент или Railway CLI

---

## 🌐 Шаг 3: Регистрация на Render.com

1. **Зайдите на https://render.com**
2. **Нажмите "Get Started for Free"**
3. **Войдите через GitHub** (рекомендуется) - это упростит подключение репозитория

---

## 🚀 Шаг 4: Создание Web Service на Render

1. **В Dashboard Render нажмите "New +" → "Web Service"**

2. **Подключите GitHub репозиторий:**
   - Если GitHub еще не подключен:
     - Нажмите "Connect account"
     - Авторизуйтесь через GitHub
     - Разрешите доступ к репозиториям
   - Выберите репозиторий: `25angel/autoservice`
   - Нажмите "Connect"

3. **Настройте сервис:**
   - **Name**: `autoservice` (или любое другое имя)
   - **Region**: выберите ближайший (например, `Oregon (US West)`)
   - **Branch**: `master` (или `main`, если у вас main)
   - **Root Directory**: оставьте пустым
   - **Environment**: выберите **`PHP`**
   - **Build Command**: `composer install`
   - **Start Command**: `php -S 0.0.0.0:$PORT`

4. **Настройте переменные окружения:**
   
   Нажмите "Advanced" → прокрутите вниз до "Environment Variables"
   
   **Если используете PlanetScale или Railway с отдельными переменными:**
   
   Добавьте переменные (нажмите "Add Environment Variable" для каждой):
   ```
   DB_HOST = ваш_хост (например, aws.connect.psdb.cloud)
   DB_PORT = 3306
   DB_DATABASE = autoservice
   DB_USERNAME = ваш_username
   DB_PASSWORD = ваш_пароль
   ```
   
   **ИЛИ если есть строка подключения (DATABASE_URL):**
   
   Добавьте одну переменную:
   ```
   DATABASE_URL = mysql://username:password@host:port/database
   ```
   
   (Вставьте скопированную строку из PlanetScale/Railway)

5. **Нажмите "Create Web Service"**

---

## ⏳ Шаг 5: Ожидание деплоя

1. **Render начнет сборку проекта:**
   - Это займет 2-5 минут
   - Вы увидите прогресс в разделе "Logs"

2. **Следите за логами:**
   - Если видите "Build successful" - отлично!
   - Если есть ошибки - проверьте:
     - Правильность переменных окружения
     - Что все файлы на GitHub
     - Что `composer.json` есть в проекте

3. **После успешного деплоя:**
   - Render даст вам URL: `https://autoservice.onrender.com`
   - (URL будет другим, если вы выбрали другое имя)

---

## ✅ Шаг 6: Проверка работы

1. **Откройте ваш URL в браузере** (например: `https://autoservice.onrender.com`)

2. **Проверьте работу сайта:**
   - Главная страница должна загрузиться
   - Попробуйте открыть каталог: `/catalog.php`
   - Попробуйте открыть услуги: `/services.php`

3. **Если видите ошибку подключения к БД:**
   - Вернитесь в Render Dashboard
   - Проверьте переменные окружения
   - Убедитесь, что БД на PlanetScale/Railway запущена

---

## 🔧 Решение проблем

### Ошибка: "Class not found" или "Composer autoload"

**Решение:**
- Убедитесь, что в Build Command указано: `composer install`
- Проверьте, что файл `composer.json` есть в репозитории
- Посмотрите логи сборки в Render Dashboard

### Ошибка: "Connection refused" или "Can't connect to MySQL"

**Решение:**
- Проверьте переменные окружения в Render Dashboard
- Убедитесь, что БД на PlanetScale/Railway запущена
- Проверьте, что используете правильный хост и порт
- Для PlanetScale: убедитесь, что используете правильный регион

### Сайт не загружается или "Application error"

**Решение:**
- Откройте "Logs" в Render Dashboard
- Посмотрите последние ошибки
- Проверьте Start Command: должно быть `php -S 0.0.0.0:$PORT`
- Убедитесь, что файл `index.php` есть в корне проекта

### Бесплатный план Render "засыпает"

**Это нормально!**
- На бесплатном плане Render останавливает сервис после 15 минут неактивности
- Первый запрос после остановки может занять 30-60 секунд (холодный старт)
- Это нормально для бесплатного плана
- Для продакшена лучше использовать платный план

---

## 📝 Важные замечания

1. **Бесплатный план Render:**
   - Сервис "засыпает" после 15 минут неактивности
   - Первый запрос после пробуждения занимает 30-60 секунд
   - Ограниченное количество часов в месяц

2. **PlanetScale Hobby план:**
   - Бесплатный
   - Ограничение на количество запросов
   - Подходит для тестирования и небольших проектов

3. **Для продакшена:**
   - Рассмотрите платный план Render
   - Или используйте другие хостинги (Heroku, DigitalOcean, AWS)

---

## 🎉 Готово!

Ваш сайт должен быть доступен по адресу, который дал Render!

**Следующие шаги:**
- Настройте домен (если нужно)
- Настройте SSL сертификат (Render делает это автоматически)
- Мониторьте логи для отладки

**Удачи! 🚀**

