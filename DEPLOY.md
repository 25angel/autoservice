# 🚀 Инструкция по деплою сайта

## ⚠️ Важно: GitHub Pages не поддерживает PHP

GitHub Pages работает только со статическими файлами (HTML, CSS, JavaScript). 
Ваш сайт использует PHP и MySQL, поэтому нужен другой хостинг.

## 📋 Варианты деплоя

### Вариант 1: Render.com (РЕКОМЕНДУЕТСЯ - бесплатный)

1. **Создайте аккаунт на Render.com**: https://render.com
2. **Подготовьте репозиторий на GitHub**:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ВАШ_ЛОГИН/php-project.git
   git push -u origin main
   ```

3. **На Render.com**:
   - Создайте новый "Web Service"
   - Подключите ваш GitHub репозиторий
   - Настройки:
     - Build Command: `composer install`
     - Start Command: `php -S 0.0.0.0:$PORT`
     - Environment: PHP

4. **Создайте базу данных MySQL на Render**:
   - Создайте "PostgreSQL" или используйте внешний MySQL (например, от PlanetScale или Railway)
   - Получите строку подключения

5. **Настройте переменные окружения**:
   ```
   DB_HOST=ваш_хост
   DB_PORT=3306
   DB_DATABASE=autoservice
   DB_USERNAME=ваш_логин
   DB_PASSWORD=ваш_пароль
   ```

### Вариант 2: Railway.app (бесплатный)

1. **Создайте аккаунт**: https://railway.app
2. **Подключите GitHub репозиторий**
3. **Добавьте MySQL базу данных**
4. **Настройте переменные окружения** (как в варианте 1)

### Вариант 3: Heroku (бесплатный план закрыт, но есть альтернативы)

Heroku больше не предоставляет бесплатный план, но можно использовать:
- **Fly.io** (бесплатный)
- **Koyeb** (бесплатный)
- **000webhost** (бесплатный PHP хостинг)

### Вариант 4: Бесплатные PHP хостинги

- **000webhost.com** - бесплатный PHP + MySQL
- **InfinityFree.net** - бесплатный PHP + MySQL
- **Freehostia.com** - бесплатный PHP + MySQL

## 📝 Что нужно сделать перед деплоем

1. **Создайте `.gitignore`** (если еще нет):
   ```
   /vendor/
   .env
   *.log
   ```

2. **Создайте файл `.htaccess`** для Apache (если используете):
   ```apache
   RewriteEngine On
   RewriteCond %{REQUEST_FILENAME} !-f
   RewriteCond %{REQUEST_FILENAME} !-d
   RewriteRule ^(.*)$ index.php [QSA,L]
   ```

3. **Обновите `config/database.php`** для использования переменных окружения (уже сделано)

## 🔧 Быстрый старт с Render.com

**📖 Подробная инструкция находится в файле `RENDER_SETUP.md`**

Кратко:
1. Зарегистрируйтесь на https://render.com
2. Создайте MySQL базу данных на Render
3. Импортируйте `database/schema-render.sql` в БД
4. Создайте Web Service и подключите GitHub репозиторий
5. Настройте переменные окружения для подключения к БД
6. Дождитесь деплоя

**Важно:** Используйте файл `database/schema-render.sql` (без CREATE DATABASE), а не `schema.sql`

## 📦 Альтернатива: Статическая версия (ограниченная)

Если очень нужно использовать GitHub Pages, можно создать статическую версию:
- Конвертировать PHP в статический HTML
- Использовать JSON файлы вместо БД
- Потеряется функциональность корзины и заказов

**НЕ РЕКОМЕНДУЕТСЯ** для вашего проекта, так как потеряется вся динамика.

