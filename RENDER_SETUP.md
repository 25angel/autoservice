# 🚀 Пошаговая инструкция: Деплой на Render.com с MySQL

## 📋 Шаг 1: Подготовка репозитория на GitHub

1. **Инициализируйте Git** (если еще не сделано):
   ```bash
   cd /Users/skyapathy/php-project
   git init
   git add .
   git commit -m "Initial commit - готово для деплоя на Render"
   ```

2. **Создайте репозиторий на GitHub**:
   - Зайдите на https://github.com
   - Нажмите "New repository"
   - Название: `php-project` (или любое другое)
   - НЕ ставьте галочки (README, .gitignore, license - у нас уже есть)
   - Нажмите "Create repository"

3. **Заливайте код на GitHub**:
   ```bash
   git remote add origin https://github.com/ВАШ_ЛОГИН/php-project.git
   git branch -M main
   git push -u origin main
   ```

## 📋 Шаг 2: Регистрация на Render.com

1. Зайдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub (рекомендуется) - это упростит подключение репозитория

## 📋 Шаг 3: Создание MySQL базы данных на Render

1. **В Dashboard Render нажмите "New +" → "PostgreSQL"** (да, PostgreSQL, но Render также поддерживает MySQL через внешние сервисы)

   **ВАРИАНТ А: Использовать встроенный MySQL (если доступен)**
   - Если видите "MySQL" в списке - выберите его
   - Name: `autoservice-db`
   - Database: `autoservice`
   - User: `autoservice_user`
   - Region: выберите ближайший
   - Plan: Free (для начала)
   - Нажмите "Create Database"

   **ВАРИАНТ Б: Использовать внешний MySQL (PlanetScale, Railway, или другой)**
   - Создайте MySQL на PlanetScale.com (бесплатно) или Railway.app
   - Получите строку подключения

2. **После создания БД, Render покажет переменные окружения:**
   - `MYSQL_HOST` или `DB_HOST`
   - `MYSQL_PORT` или `DB_PORT` (обычно 3306)
   - `MYSQL_DATABASE` или `DB_DATABASE`
   - `MYSQL_USER` или `DB_USERNAME`
   - `MYSQL_PASSWORD` или `DB_PASSWORD`
   - Или `DATABASE_URL` (строка подключения)

   **Скопируйте эти значения!** Они понадобятся на следующем шаге.

## 📋 Шаг 4: Импорт данных в базу данных

### Способ 1: Через Render Dashboard (если есть доступ к консоли)

1. В Dashboard вашей БД на Render найдите "Connect" или "Internal Database URL"
2. Используйте встроенную консоль или подключитесь через внешний клиент

### Способ 2: Через локальный MySQL клиент

1. **Получите External Connection String** из Render Dashboard
2. **Подключитесь локально:**
   ```bash
   mysql -h ВАШ_ХОСТ -P 3306 -u ВАШ_ПОЛЬЗОВАТЕЛЬ -p ВАША_БД < database/schema-render.sql
   ```
   Введите пароль, когда попросит

### Способ 3: Через phpMyAdmin или другой GUI

1. Используйте External Connection String из Render
2. Подключитесь через phpMyAdmin, MySQL Workbench, или DBeaver
3. Импортируйте файл `database/schema-render.sql`

## 📋 Шаг 5: Создание Web Service на Render

1. **В Dashboard Render нажмите "New +" → "Web Service"**

2. **Подключите GitHub репозиторий:**
   - Выберите "Connect account" если еще не подключен
   - Выберите ваш репозиторий `php-project`
   - Нажмите "Connect"

3. **Настройте сервис:**
   - **Name**: `autoservice` (или любое другое)
   - **Region**: выберите тот же регион, что и БД
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым (корень проекта)
   - **Environment**: `PHP`
   - **Build Command**: `composer install`
   - **Start Command**: `php -S 0.0.0.0:$PORT`

4. **Настройте переменные окружения:**
   Нажмите "Advanced" → "Add Environment Variable" и добавьте:

   Если Render предоставил отдельные переменные:
   ```
   DB_HOST = значение из шага 3
   DB_PORT = 3306 (или значение из шага 3)
   DB_DATABASE = значение из шага 3
   DB_USERNAME = значение из шага 3
   DB_PASSWORD = значение из шага 3
   ```

   ИЛИ если есть DATABASE_URL:
   ```
   DATABASE_URL = значение из шага 3
   ```

5. **Важно: Подключите БД к Web Service:**
   - В разделе "Environment" найдите "Add Database"
   - Выберите вашу БД `autoservice-db`
   - Render автоматически добавит переменные окружения

6. **Нажмите "Create Web Service"**

## 📋 Шаг 6: Ожидание деплоя

1. Render начнет сборку проекта (может занять 2-5 минут)
2. Следите за логами в разделе "Logs"
3. Если есть ошибки - проверьте:
   - Правильность переменных окружения
   - Что БД импортирована
   - Что все файлы загружены на GitHub

## 📋 Шаг 7: Проверка работы

1. После успешного деплоя Render даст вам URL: `https://autoservice.onrender.com`
2. Откройте его в браузере
3. Проверьте:
   - Главная страница загружается
   - Каталог запчастей работает
   - Корзина работает
   - Заказы создаются

## 🔧 Решение проблем

### Ошибка подключения к БД
- Проверьте переменные окружения в Render Dashboard
- Убедитесь, что БД создана и запущена
- Проверьте, что импортирован `schema-render.sql`

### Ошибка "Class not found"
- Убедитесь, что `composer install` выполняется в Build Command
- Проверьте, что `vendor/` не в `.gitignore` (или что он загружается)

### Сайт не загружается
- Проверьте логи в Render Dashboard
- Убедитесь, что Start Command правильный: `php -S 0.0.0.0:$PORT`
- Проверьте, что порт использует переменную `$PORT`

### Бесплатный план Render "засыпает"
- На бесплатном плане Render останавливает сервис после 15 минут неактивности
- Первый запрос после остановки может занять 30-60 секунд (холодный старт)
- Это нормально для бесплатного плана

## 💡 Полезные ссылки

- Render Dashboard: https://dashboard.render.com
- Render Docs: https://render.com/docs
- Render Community: https://community.render.com

## 📝 Примечания

- **Бесплатный план Render** имеет ограничения:
  - Сервис "засыпает" после 15 минут неактивности
  - Ограниченное количество часов в месяц
  - Для продакшена лучше использовать платный план

- **База данных на Render**:
  - Бесплатная БД может быть удалена через 90 дней неактивности
  - Для постоянного использования рассмотрите платный план или внешний MySQL

- **Альтернативы Render для MySQL**:
  - PlanetScale.com (бесплатный MySQL)
  - Railway.app (бесплатный MySQL)
  - Aiven.io (бесплатный пробный период)

