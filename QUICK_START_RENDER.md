Шаг 1: Проверка установки XAMPP

1. **Откройте Проводник Windows:**
   - Нажмите клавиши `Win + E` (Win - это клавиша с логотипом Windows)
   - Или найдите "Проводник" в меню Пуск

2. **Перейдите в папку XAMPP:**
   - В адресной строке Проводника (вверху) введите: `C:\xampp`
   - Нажмите Enter
   - Или просто откройте диск C: и найдите папку `xampp`

3. **Проверьте наличие папок:**
   - Должна быть папка `php` - это PHP (язык программирования)
   - Должна быть папка `mysql` - это база данных
   - Должна быть папка `apache` - это веб-сервер

✅ **Если все папки есть** - отлично! XAMPP установлен правильно. Переходите к следующему шагу.

❌ **Если папок нет** - значит XAMPP не установлен или установлен в другом месте. Установите XAMPP с сайта https://www.apachefriends.org/

---

## 🎮 Шаг 2: Запуск XAMPP

**Что мы делаем:** Запускаем программы, которые нужны для работы сайта (Apache и MySQL).

1. **Найдите программу XAMPP Control Panel:**
   - Нажмите клавишу `Win` (клавиша с логотипом Windows)
   - В поиске введите: `xampp`
   - Найдите и откройте **"XAMPP Control Panel"**
   - Или откройте файл напрямую: `C:\xampp\xampp-control.exe` (двойной клик)

2. **Откроется окно XAMPP Control Panel:**
   - Вы увидите список программ: Apache, MySQL, FileZilla и другие
   - Нам нужны только **Apache** и **MySQL**

3. **Запустите Apache:**
   - Найдите строку с названием **"Apache"**
   - Справа от нее нажмите кнопку **"Start"**
   - Подождите 2-3 секунды
   - Кнопка станет **зеленой**, появится надпись **"Running"** (Работает)
   - ⚠️ **Если появилось окно брандмауэра Windows:**
     - Нажмите кнопку **"Разрешить доступ"** или **"Allow access"**
     - Это нормально, Windows спрашивает разрешение для работы программы

4. **Запустите MySQL:**
   - Найдите строку с названием **"MySQL"**
   - Справа от нее нажмите кнопку **"Start"**
   - Подождите 2-3 секунды
   - Кнопка станет **зеленой**, появится надпись **"Running"** (Работает)

✅ **Готово!** Теперь Apache и MySQL работают. Окно XAMPP Control Panel можно оставить открытым или свернуть (не закрывайте!).

---

## 📁 Шаг 3: Размещение проекта

**Что мы делаем:** Копируем папку проекта в удобное место, чтобы сайт мог работать.

1. **Найдите папку проекта `php-project`:**
   - Откройте Проводник Windows (`Win + E`)
   - Найдите папку `php-project` (там, где вы ее скачали или распаковали)

2. **Скопируйте папку проекта:**
   - Правой кнопкой мыши на папке `php-project`
   - Выберите **"Копировать"** (Copy)

3. **Вставьте папку в нужное место:**
   
   **Вариант А (рекомендуется):** В папку XAMPP
   - Откройте Проводник
   - Перейдите в: `C:\xampp\htdocs`
   - Правой кнопкой мыши в пустом месте → **"Вставить"** (Paste)
   - Папка скопируется в `C:\xampp\htdocs\php-project`
   
   **Вариант Б:** В любое другое место
   - Можете скопировать в любое место, например: `C:\Users\ВашеИмя\php-project`
   - Просто запомните, куда вы скопировали

4. **Запомните путь к проекту:**
   - Запишите на листочек или запомните полный путь
   - Например: `C:\xampp\htdocs\php-project`
   - Этот путь понадобится в следующих шагах

✅ **Готово!** Проект скопирован.

---

## 🔧 Шаг 4: Установка зависимостей (Composer)

**Что мы делаем:** Устанавливаем дополнительные библиотеки, которые нужны для работы проекта.

1. **Откройте командную строку Windows:**
   - Нажмите клавиши `Win + R` одновременно
   - В открывшемся окне введите: `cmd`
   - Нажмите Enter или кнопку "ОК"
   - Откроется черное окно - это командная строка

2. **Перейдите в папку проекта:**
   
   **Вариант А:** Если проект в `C:\xampp\htdocs\php-project`
   - В командной строке введите (или скопируйте и вставьте):
   ```
   cd C:\xampp\htdocs\php-project
   ```
   - Нажмите Enter
   
   **Вариант Б:** Если проект в другом месте (например, `C:\Users\Иван\php-project`)
   - Замените путь на ваш:
   ```
   cd C:\Users\Иван\php-project
   ```
   - Нажмите Enter
   
   **Как вставить команду в командную строку:**
   - Скопируйте команду (выделите и `Ctrl + C`)
   - В командной строке правой кнопкой мыши → **"Вставить"**
   - Или просто `Shift + Insert`
   
   **Как проверить, что вы в правильной папке:**
   - После команды `cd` в командной строке должен появиться путь к проекту
   - Или введите команду `dir` и нажмите Enter
   - Вы должны увидеть файлы проекта: `index.php`, `cart.php`, `composer.json` и другие

3. **Установите зависимости:**
   - Скопируйте и вставьте эту команду:
   ```
   C:\xampp\php\php.exe C:\xampp\php\composer.phar install
   ```
   - Нажмите Enter
   - Подождите 1-2 минуты (может быть дольше при первом запуске)
   - Вы увидите много текста - это нормально, идет установка
   
   ⚠️ **Если появилась ошибка "composer.phar не найден":**
   - Откройте браузер и перейдите на: https://getcomposer.org/download/
   - Скачайте файл `Composer-Setup.exe`
   - Запустите его и установите Composer (просто нажимайте "Далее" везде)
   - После установки закройте командную строку и откройте новую
   - Снова перейдите в папку проекта командой `cd`
   - Теперь используйте команду: `composer install`

4. **Дождитесь окончания установки:**
   - В конце вы должны увидеть сообщение: `Generating autoload files`
   - Это значит, что все установлено успешно


---

## 🗄️ Шаг 5: Создание базы данных и импорт данных

**Что мы делаем:** Создаем базу данных (хранилище информации) и заполняем ее начальными данными.

**⚠️ Важно:** Убедитесь, что MySQL запущен в XAMPP Control Panel (зеленая кнопка "Running"). Если не запущен - вернитесь к Шагу 2.

1. **Откройте командную строку** (если закрыли):
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите Enter

2. **Создайте базу данных:**
   - Скопируйте и вставьте эту команду (правой кнопкой мыши в командной строке → "Вставить"):
   ```
   C:\xampp\mysql\bin\mysql.exe -u root -e "CREATE DATABASE IF NOT EXISTS autoservice CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
   ```
   - Нажмите Enter
   - Если все хорошо, вы просто вернетесь к приглашению командной строки (без ошибок)
   - Если появилась ошибка - проверьте, что MySQL запущен

3. **Импортируйте данные в базу:**

   **Сначала узнайте, где находится ваш проект:**
   - Откройте Проводник Windows (`Win + E`)
   - Найдите папку `php-project`
   - Правой кнопкой мыши на папке → **"Свойства"**
   - В открывшемся окне найдите поле **"Расположение"** (Location)
   - Скопируйте этот путь (например: `C:\xampp\htdocs\php-project`)
   - Или просто запомните его

   **Теперь выполните команду импорта:**
   
   **Вариант А:** Если проект находится в `C:\xampp\htdocs\php-project`
   - Скопируйте и вставьте эту команду:
   ```
   C:\xampp\mysql\bin\mysql.exe -u root autoservice < C:\xampp\htdocs\php-project\database\schema.sql
   ```
   - Нажмите Enter
   
   **Вариант Б:** Если проект находится в другом месте (например, `C:\Users\Иван\php-project`)
   - Замените путь в команде на ваш путь:
   ```
   C:\xampp\mysql\bin\mysql.exe -u root autoservice < C:\Users\Иван\php-project\database\schema.sql
   ```
   - Скопируйте команду с вашим путем и нажмите Enter

   **Как понять, что все получилось:**
   - Если команда выполнилась успешно, вы просто вернетесь к приглашению командной строки (без ошибок)
   - Никаких сообщений не будет - это нормально!
   - Если появилась ошибка (красный текст) - проверьте, что путь к файлу правильный и файл `schema.sql` существует в папке `database`

✅ **Готово!** База данных создана и заполнена данными. Теперь в ней есть все нужные таблицы с информацией.

---

## ⚙️ Шаг 6: Настройка подключения к базе данных

**Что мы делаем:** Проверяем настройки подключения к базе данных (чтобы сайт знал, как к ней подключиться).

1. **Откройте файл конфигурации:**
   - Откройте Проводник Windows (`Win + E`)
   - Перейдите в папку проекта (например: `C:\xampp\htdocs\php-project`)
   - Откройте папку `config`
   - Найдите файл `database.php`
   - Правой кнопкой мыши на файле → **"Открыть с помощью"** → **"Блокнот"** (Notepad)
   - Или просто дважды кликните, если Блокнот установлен по умолчанию

2. **Проверьте настройки:**
   - Файл откроется в Блокноте
   - Вы увидите код на PHP - это нормально
   - **Для XAMPP по умолчанию используются такие настройки:**
     - Имя пользователя: `root`
     - Пароль: пустой (без пароля)
   - Эти настройки уже должны быть правильными в файле
   - **Если у вас установлен пароль для MySQL** (вы его сами устанавливали):
     - Найдите в файле строку с `'password' => ''`
     - Замените пустые кавычки на ваш пароль: `'password' => 'ваш_пароль'`

3. **Сохраните файл (если что-то меняли):**
   - Нажмите `Ctrl + S` (удерживайте Ctrl и нажмите S)
   - Или меню **"Файл"** → **"Сохранить"**
   - Закройте Блокнот

✅ **Настройки готовы!** Теперь сайт знает, как подключиться к базе данных.

---

## 🌐 Шаг 7: Запуск сайта

**Что мы делаем:** Запускаем сайт, чтобы его можно было открыть в браузере.

### Вариант 1: Через встроенный сервер PHP (рекомендуется для тестирования)

1. **Откройте командную строку:**
   - Нажмите клавиши `Win + R`
   - Введите `cmd` и нажмите Enter
   - Или найдите "Командная строка" в меню Пуск

2. **Перейдите в папку проекта:**
   
   **Вариант А:** Если проект находится в `C:\xampp\htdocs\php-project`
   - Скопируйте и вставьте эту команду:
   ```
   cd C:\xampp\htdocs\php-project
   ```
   - Нажмите Enter
   
   **Вариант Б:** Если проект находится в другом месте (например, `C:\Users\Иван\php-project`)
   - Замените путь на ваш путь:
   ```
   cd C:\Users\Иван\php-project
   ```
   - Скопируйте команду с вашим путем, вставьте и нажмите Enter
   
   **Как проверить, что вы в правильной папке:**
   - После команды `cd` в командной строке должен появиться путь к проекту
   - Или введите команду `dir` и нажмите Enter
   - Вы должны увидеть файлы проекта: `index.php`, `cart.php`, `catalog.php` и другие

3. **Запустите сервер:**
   - Скопируйте и вставьте эту команду:
   ```
   C:\xampp\php\php.exe -S localhost:8000
   ```
   - Нажмите Enter
   - Вы должны увидеть сообщение примерно такое: `PHP 8.x.x Development Server started at http://localhost:8000`
   - **⚠️ ВАЖНО:** Окно командной строки должно оставаться открытым! Не закрывайте его, иначе сайт перестанет работать
   - Можете свернуть окно, но не закрывать

4. **Откройте сайт в браузере:**
   - Откройте браузер (Chrome, Firefox, Edge - любой)
   - В адресной строке браузера (вверху, где обычно пишется адрес сайта) введите:
   ```
   http://localhost:8000
   ```
   - Нажмите Enter
   - Сайт должен открыться!

✅ **Сайт работает!** Вы должны увидеть главную страницу сайта автосервиса.

⚠️ **Важно:** 
- Пока вы работаете с сайтом, **не закрывайте окно командной строки**! Если закроете - сайт перестанет работать.
- Чтобы остановить сайт, вернитесь в окно командной строки и нажмите `Ctrl + C`

